<script src="https://unpkg.com/hydra-synth"></script>
<canvas id="myCanvas" width="1000px" height="800px"></canvas>
<script>
  // create a new hydra-synth instance
  var hydra = new Hydra({canvas: document.getElementById("myCanvas"), detectAudio: false})
  osc(50, 0.01)
    .diff(osc(50, 0.02).rotate(Math.PI / 2))
    .pixelate(30)
    .color(.12, .12, .8)
    .out()
  /* src(o0)
    .saturate(1.01)
    .scale(.1999)
    .color(1.01, 1.01, 1.01)
    .hue(.01)
    .modulateHue(src(o1).hue(.3).posterize(-1).contrast(.7), 2)
    .layer(src(o1)
        .luma()
        .mult(gradient(1)
            .saturate(.9)))
    .out(o0)

   noise(1, .2)
    .rotate(2, .5)
    .layer(src(o0)
        .scrollX(.2))
    .out(o1)

   render(o0) */
</script>